{
  "scripts": {
    "start": "cross-env NODE_ENV=development nodemon index.js",
    "prod:local": "NODE_ENV=production node index.js",
    "prod:server": "NODE_ENV=production pm2 start index.js --name postges-demo"
  },
  "dependencies": {
    "dotenv": "^8.0.0",
    "pg": "^7.12.0",
    "pg-hstore": "^2.3.3",
    "sequelize": "^5.12.2"
  },
  "devDependencies": {
    "cross-env": "^5.2.0",
    "nodemon": "^1.19.1"
  }
}
